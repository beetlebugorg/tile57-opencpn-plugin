# Dependencies
/node_modules

# Lockfiles — CI uses `npm install` (see docs.yml); don't commit a lockfile.
/package-lock.json
/bun.lock

# Production build
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
